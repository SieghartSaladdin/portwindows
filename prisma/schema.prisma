datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "../src/generated/client"
}

model Profile {
  id          String  @id @default("1")
  name        String
  title       String
  location    String
  email       String
  bio         String
  githubUrl   String?
  linkedinUrl String?
}

model Project {
  id          String  @id @default(uuid())
  title       String
  description String
  tags        Json    // Store as JSON array of strings
  githubUrl   String?
  liveUrl     String?
  images      Json    @default("[]") // Array of image URLs/paths
  featured    Boolean @default(false) // Recommended project banner flag
}

model Skill {
  id       String @id @default(uuid())
  category String
  skills   Json    // Store as JSON array of strings
}

model Experience {
  id          String @id @default(uuid())
  role        String
  company     String
  duration    String
  description Json   // Store as JSON array of strings
}
